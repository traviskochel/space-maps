const React = require('react');
// const Mapper = require()
const MapWrapper = require('./Map/MapWrapper');

const App = React.createClass({
  render: function() {
    return (
      <MapWrapper />
    );
  }
})

module.exports = App;
